import {ContentContainer} from "./ContentStyles";

const Content = ({children}) => {
    return (
        <ContentContainer>
            {children}
        </ContentContainer>
    )
}

export default Content;