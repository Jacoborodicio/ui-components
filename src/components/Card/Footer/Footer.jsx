import {FooterContainer} from "./FooterStyles";

const Footer = ({children}) => {
    return (
        <FooterContainer>
            {children}
        </FooterContainer>
    )
}

export default Footer;